package com.example.bangunan.widgets;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.LinearLayout;

import androidx.annotation.Nullable;

public class wrapElement extends LinearLayout {
    public wrapElement(Context context) {
        super(context);
    }

    public wrapElement(Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }

    public wrapElement(Context context, @Nullable AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }

    public wrapElement(Context context, AttributeSet attrs, int defStyleAttr, int defStyleRes) {
        super(context, attrs, defStyleAttr, defStyleRes);
    }
}
